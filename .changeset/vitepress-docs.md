---
---

Add VitePress documentation site with auto-generated CLI command references. Adds `docs:dev`, `docs:build`, `docs:generate`, and `docs:preview` scripts to root package.json. Documentation is generated from `--llms` manifests and deployed via GitHub Pages.
