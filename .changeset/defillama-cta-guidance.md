---
"@spectratools/defillama-cli": patch
---

Add call-to-action guidance to all DefiLlama read commands so outputs suggest meaningful next commands (for example drilling from overviews into protocol detail, chain filtering, and price trend follow-ups).
