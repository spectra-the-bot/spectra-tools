---
"@spectratools/erc8004-cli": minor
---

Add identity burn command for permanently destroying agent identity tokens. Currently returns BURN_NOT_SUPPORTED as the IdentityRegistry contract does not expose a public burn function.
