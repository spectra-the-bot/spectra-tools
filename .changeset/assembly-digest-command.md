---
"@spectratools/assembly-cli": minor
---

Add digest command that aggregates proposals, threads, comments, petitions, and members into a single JSON snapshot with optional wallet-relative enrichment.
